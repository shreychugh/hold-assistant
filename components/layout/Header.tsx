// Header Component
// Top navigation bar for the Hold Assistant app.
// Will contain the logo, app name, and nav links.
// Phase 2: Will include user account menu when auth is added.

export default function Header() {
  return (
    <header>
      <nav>
        <span>Hold Assistant</span>
      </nav>
    </header>
  );
}
